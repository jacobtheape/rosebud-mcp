mcp>=2
uvicorn>=0.30
starlette>=0.37
# Live Stripe verification/refunds (live mode only):
stripe>=7.0
# Persistent quote/payment store (ROSEBUD_STORE=postgres, production):
psycopg[binary]>=3.1
psycopg-pool>=3.1
# Live browser checkout only (fake mode, the default, doesn't need it):
# playwright>=1.40  +  playwright install chromium
